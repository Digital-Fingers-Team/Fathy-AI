import SignupClient from "@/components/SignupClient";

export const metadata = {
  title: "Sign Up - Fathy",
  description: "Create a new Fathy account"
};

export default function SignupPage() {
  return <SignupClient />;
}
